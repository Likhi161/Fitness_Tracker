apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: likhitha-pvc
  namespace: app-ns
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: mongodb-storage   
  resources:
    requests:
      storage: 10Gi